# OmniCell-AI Phase 3 — Agent Swarm Dependencies
# Install with: uv pip install -r requirements.txt

# --- LangGraph & LangChain Core ---
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0          # LLM backend (xAI Grok uses OpenAI-compatible API)

# --- Neo4j GraphRAG Driver ---
neo4j>=5.12.0

# --- Kafka Consumer ---
confluent-kafka>=2.3.0

# --- Utilities ---
python-dotenv>=1.0.0             # .env config loading
pydantic>=2.0.0                  # TypedDict / state validation